version: '3.8'

services:
  mov2mp4:
    build: .
    container_name: mov2mp4
    environment:
      - WATCH_DIR=/watch
    volumes:
      - "${HOST_WATCH_DIR:-./videos}:/watch"
    restart: unless-stopped
